# Microshare API Credentials
MICROSHARE_USERNAME=your_username@company.com
MICROSHARE_PASSWORD=your_secure_password
MICROSHARE_API_KEY=YOUR-API-KEY-UUID-HERE
MICROSHARE_DATA_CONTEXT=YourOrganization

# Azure Event Hub Connection
# Format: Endpoint=sb://namespace.servicebus.windows.net/;SharedAccessKeyName=name;SharedAccessKey=key;EntityPath=hubname
EVENT_HUB_CONNECTION_STRING=Endpoint=sb://your-namespace.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=YOUR_ACCESS_KEY_HERE;EntityPath=your-eventhub-name

# Logging Level (INFO, DEBUG, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# OPTIONAL: Advanced Configuration
# =============================================================================
# These are typically configured in config.yaml, but can be overridden via
# environment variables if needed

# Microshare View ID (get from Microshare support)
# MICROSHARE_VIEW_ID=your-view-id-here

# Record Type - Choose based on your data source:
# - io.microshare.lake.snapshot.hourly (hourly occupancy snapshots)
# - io.microshare.peoplecounter.unpacked.event.agg (15-min people counting)
# - io.microshare.occupancy.unpacked (motion sensor data)
# MICROSHARE_REC_TYPE=io.microshare.lake.snapshot.hourly

# Location Filter (optional - filters data by building/location)
# MICROSHARE_LOCATION=Building-A
